# Claude Code Notes

## Development Setup

For development environment setup (starting the server, Docker commands, etc.), see **DEV-SETUP.md**.

Quick reference:
- Start dev server: `docker compose up -d`
- View logs: `docker compose logs -f`
- Dev URL: http://localhost:1111
